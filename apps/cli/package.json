{
  "name": "@opencode-packman/cli",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "bin": {
    "opm": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx src/index.ts",
    "lint": "eslint src --ext .ts",
    "lint:fix": "eslint src --ext .ts --fix",
    "test": "echo 'cli only'"
  },
  "dependencies": {
    "@opencode-packman/core": "workspace:*",
    "chalk": "^5.6.0",
    "commander": "^12.1.0",
    "fs-extra": "^11.3.1",
    "prompts": "^2.4.2"
  },
  "devDependencies": {
    "@types/prompts": "^2.4.9",
    "tsx": "^4.20.3"
  }
}
